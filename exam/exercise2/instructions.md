# Exercise 2 Instructions

Write a C program that:

- Prompts the user for how many numbers will be entered.
- Asks the user to enter that many integers.
- Adds up all the numbers.
- Places this logic in a function called `sum`.
- Calls `sum` from `main` and prints the result.
